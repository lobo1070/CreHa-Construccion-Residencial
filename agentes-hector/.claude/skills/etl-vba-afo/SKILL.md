---
name: etl-vba-afo
description: "STUB — pendiente de contenido real. Patrón de Hector para el workaround de ETL SAP vía VBA + Analysis for Office. No usar como fuente de verdad hasta completar el TODO."
---

# ETL VBA + Analysis for Office (patrón SAP) — STUB

Este skill está pendiente de contenido real. Hector tiene documentado un patrón propio (mencionado en el handbook original, secciones 3.5 y 7) para el workaround de ETL cuando el origen es SAP, usando VBA + Analysis for Office.

## TODO

Reemplazar este archivo con el patrón real de Hector (macro VBA + estructura de la hoja AfO + pasos de uso) antes de que `pipeline-etl` (`.claude/agents/pipeline-etl.md`) lo use como fuente de verdad.

Hasta entonces, `pipeline-etl` **pregunta explícitamente a Hector** si el patrón SAP aplica al caso en cuestión, en vez de asumir o inventar el workaround.
